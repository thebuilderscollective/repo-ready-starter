// Example test locations only: no application exists yet.
// Run with Node.js: node --test tests/*.test.mjs
// These TODOs perform no assertions and provide no test coverage.
// Replace each with a real test of your application before enabling CI.
import { test } from "node:test";

test.todo("a valid waitlist signup is stored once");
test.todo("a repeated signup does not create a duplicate");
test.todo("invalid input gets a useful error");
